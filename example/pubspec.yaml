name: samseer_example
description: Example showcasing samseer with Dio, http, and HttpClient.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.0
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.4.0
  http: ^1.2.0
  samseer:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
